{
  "name": "micro-portalen-links",
  "description": "Mapping of links",
  "version": "1.1.0",
  "license": "MIT",
  "author": {
    "name": "Geir Gåsodden",
    "email": "geir.gasodden@pythonia.no",
    "url": "https://github.com/zrrrzzt"
  },
  "main": "index.js",
  "engines": {
    "node": ">=7.5.0"
  },
  "scripts": {
    "test": "standard && nsp check && tap --reporter=spec test/**/*.js",
    "test-offline": "standard && tap --reporter=spec test/**/*.js",
    "coverage": "tap test/**/*.js --coverage",
    "coveralls": "tap --cov --coverage-report=lcov test/**/*.js && cat coverage/lcov.info | coveralls",
    "setup": "npm install",
    "standard-fix": "standard --fix",
    "start": "micro -p 8080",
    "dev": "micro",
    "now-deploy": "npm test && now --npm --alias"
  },
  "keywords": [
    "microservice",
    "micro",
    "links",
    "Telemark fylkeskommune"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/telemark/micro-portalen-links.git"
  },
  "bugs": {
    "url": "https://github.com/telemark/micro-portalen-links/issues"
  },
  "now": {
    "aliases": [
      "links.portalen.t-fk.win"
    ],
    "env": {
      "NODE_ENV": "production"
    }
  },
  "devDependencies": {
    "coveralls": "2.11.16",
    "nsp": "2.6.2",
    "standard": "8.6.0",
    "tap": "10.1.0"
  },
  "dependencies": {
    "is-file": "1.0.0",
    "marked": "0.3.6",
    "micro": "7.0.4",
    "tfk-unique-array": "1.0.7"
  }
}
